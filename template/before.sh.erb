# Export the module function if it exists
[[ $(type -t module) == "function" ]] && export -f module
export port=$(find_port)
export guacdport=$(find_port)
export username=$(getent passwd $(id -u) | cut -d: -f1)
